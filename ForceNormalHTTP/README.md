_Not finished!_

As of version 0.66.2, Rōblox Freedom Distribution requires the use of unsigned HTTPS for the web server,

This guide will outline how to force all server traffic to route through HTTP.

Once a patch is implemented, RFD webservers will change to accept simple HTTP, reducing line count.
